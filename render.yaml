services:
  - type: web
    name: atmos-weather
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn --bind 0.0.0.0:$PORT "weather_app:create_server()"
    envVars:
      - key: OPENWEATHER_API_KEY
        sync: false
